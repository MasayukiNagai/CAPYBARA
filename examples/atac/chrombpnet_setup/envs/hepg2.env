#!/usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../../../.." && pwd)"

CELL_TYPE="HepG2"
BASE_DIR="${BASE_DIR:-${REPO_ROOT}/data/chrombpnet}"
THREADS="${THREADS:-4}"

BAM_ACCESSIONS=(
  ENCFF239RGZ
  ENCFF394BBD
)

PEAKS_EXPERIMENT="${PEAKS_EXPERIMENT:-ENCSR042AWH}"
CALL_PEAKS="${CALL_PEAKS:-0}"
