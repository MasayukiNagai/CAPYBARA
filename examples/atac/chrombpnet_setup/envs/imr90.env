#!/usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../../../.." && pwd)"

CELL_TYPE="IMR90"
BASE_DIR="${BASE_DIR:-${REPO_ROOT}/data/chrombpnet}"
THREADS="${THREADS:-4}"

BAM_ACCESSIONS=(
  ENCFF848XMR
  ENCFF715NAV
)

PEAKS_EXPERIMENT="${PEAKS_EXPERIMENT:-ENCSR200OML}"
CALL_PEAKS="${CALL_PEAKS:-0}"
