#!/usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../../../.." && pwd)"

CELL_TYPE="GM12878"
BASE_DIR="${BASE_DIR:-${REPO_ROOT}/data/chrombpnet}"
THREADS="${THREADS:-4}"

BAM_ACCESSIONS=(
  ENCFF981FXV
  ENCFF962FMH
  ENCFF440GRZ
)

PEAKS_EXPERIMENT="${PEAKS_EXPERIMENT:-ENCSR637XSC}"
CALL_PEAKS="${CALL_PEAKS:-0}"
